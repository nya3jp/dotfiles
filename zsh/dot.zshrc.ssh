umask 022
alias alicia='ssh -t alicia "cd `pwd`; zsh"'

agent="$HOME/.ssh_agent"
if [ ! -S "$SSH_AUTH_SOCK" ]; then
    eval `ssh-agent`
    ln -snf "$SSH_AUTH_SOCK" "$agent"
    export SSH_AUTH_SOCK="$agent"
    ssh-add
fi

